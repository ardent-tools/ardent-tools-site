# Rules to skip for specific paths
# Format: RULE/name:path/glob
#
# Every entry requires a tracking issue. No blanket category ignores.

# WHY(#128): retained-assets/ holds content-addressed, append-only copies of
# already published cast driver scripts (bin/asset_retention.py). Editing a
# historical receipt to satisfy a current shell rule would break its ledger
# hash chain. Current reproduction recipes under static/tapes/ remain owned
# source and deliberately receive no exclusion.
SHELL/*:retained-assets/**
