# Top-level Python build dependencies. Locked to bin/requirements.txt via:
#   uv pip compile --generate-hashes --universal bin/requirements.in -o bin/requirements.txt
jsonschema==4.26.0  # typikon-validate: frontmatter JSON Schema
